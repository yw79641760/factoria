# ⚠️ 架构升级通知

## v1.0 → v2.0

**v1.0（模板驱动）已废弃，请查看 v2.0（能力驱动）**

---

## 核心变化

### 架构升级

**v1.0（已废弃）**：
```
模板驱动架构
├─ 5种固定模板
├─ LLM填充变量
└─ 扩展性差
```

**v2.0（当前）**：
```
能力驱动架构
├─ Framework + Abilities + Orchestration
├─ LLM编排能力
└─ 扩展性强
```

### 核心公式

**Agent-Native Software = Framework(Abilities Orchestrated by LLM)**

```
Factoria = Framework (Abilities Orchestrated by GLM-5)
```

### 三层架构

```
Layer 1: Framework（呈现层）
├─ Web / PWA / Mobile
└─ 负责需求实现的最终呈现

Layer 2: Orchestrated by LLM（业务逻辑层）
├─ 数据挖掘 / PRD / 人工描述
└─ 将小需求实现进行有机整合

Layer 3: Abilities（能力层）
├─ 确定性能力（符号主义）
├─ 非确定性能力（连接主义）
└─ 能力复用、系统复杂性控制
```

---

## 查看新架构

📄 **[01-ARCHITECTURE-v2.md](./01-ARCHITECTURE-v2.md)** - 完整的新架构设计

---

## 关键洞察

### 为什么升级？

**v1.0 的问题**：
1. 模板边界不清晰（Calculator为例）
2. 新需求需要新模板
3. 不符合Agent-Native理念

**v2.0 的优势**：
1. 能力可沉淀，越来越丰富
2. LLM编排能力，而非填充变量
3. 支持不确定性场景
4. 符号主义 + 连接主义

### 架构对比

| 维度 | v1.0（模板） | v2.0（能力） |
|------|------------|-------------|
| **核心** | 模板填充 | 能力编排 |
| **扩展** | 需要新模板 | 沉淀新能力 |
| **LLM** | 填充变量 | 编排能力 |
| **适用** | 固定场景 | 不确定性场景 |
| **哲学** | 符号主义 | 符号+连接 |

---

## 实现示例

### v1.0（模板）
```typescript
// 硬编码的模板
const BMICalculator = () => {
  const bmi = weight / ((height/100) ** 2);  // 固定逻辑
  return <div>{bmi}</div>;
};
```

### v2.0（能力）
```typescript
// LLM编排能力
Framework.define({
  name: 'BMI计算器',
  inputs: [...],
  
  compute: (inputs, abilities) => {
    const { math, format } = abilities;  // 使用能力
    
    const height = math.divide(inputs.身高, 100);
    const bmi = math.divide(inputs.体重, math.pow(height, 2));
    
    return { bmi: format.number(bmi, 2) };
  },
  
  display: (result) => <div>{result.bmi}</div>
});
```

---

## 历史文档

- [01-ARCHITECTURE.md](./01-ARCHITECTITURE.md) - v1.0（已废弃）
- [01-ARCHITECTURE-v2.md](./01-ARCHITECTURE-v2.md) - v2.0（当前）✅

---

**升级时间**: 2026-02-26 00:12
**核心洞察**: Agent-Native Software = Framework(Abilities Orchestrated by LLM)
